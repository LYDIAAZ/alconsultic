# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains no commits, source files, or configuration beyond git initialization. There is no build system, test suite, or application code to document yet.

When code is added to this repository, update this file to include:

1. **Commands** — how to install dependencies, build, lint, run tests (including running a single test), and start the application.
2. **Architecture** — the high-level structure of the codebase: major components, how they interact, and any non-obvious design decisions that span multiple files.

Until then, treat any assumptions about language, framework, or structure as unverified — confirm with the user before proceeding with implementation work.
